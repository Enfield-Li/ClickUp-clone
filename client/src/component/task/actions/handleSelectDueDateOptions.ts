import { DueDateColumn } from "../../../types";

export function handleSelectDueDateOptions(targetColumn: DueDateColumn) {
  // const weekString = targetColumn.title;
  // const expectedDueDate = getExpectedDueDateFromWeekString(
  //   weekString as SelectableDueDate
  // );
  // setExpectedDueDate(expectedDueDate);
  // // Update list state
  // updateTaskPriorityOrDueDate(
  //   sortBy,
  //   task!,
  //   setState,
  //   DUE_DATE,
  //   targetColumn.id,
  //   expectedDueDate
  // );
  // const newEvent: UpdateEvent = {
  //   id: getRandomNumberNoLimit(),
  //   userId: authState.user?.id,
  //   taskId: task!.id!,
  //   field: DUE_DATE,
  //   beforeUpdate: String(task?.dueDate),
  //   afterUpdate: String(targetColumn.id),
  //   createdAt: new Date(),
  // };
  // // Update task state
  // if (setTask) {
  //   setTask({
  //     ...task!,
  //     expectedDueDate,
  //     dueDate: targetColumn.id,
  //     taskEvents: [...task!.taskEvents, newEvent],
  //   });
  // }
  // onClose();
}
